# Romeo Docker Environment Configuration Template
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp docker.env.example docker.env
# 2. Add your OpenAI API key below (get it from: https://platform.openai.com/api-keys)
# 3. Run: docker-compose up -d

# OpenAI Configuration (Required for the worker service)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini

# Worker Service Configuration
LOG_LEVEL=info
CORS_ORIGIN=*

# Database Configuration (already set in docker-compose.yml)
# DATABASE_URL=postgresql://romeo:romeo_password@postgres:5432/romeo

# Optional: Port overrides (uncomment to change default ports)
# POSTGRES_PORT=5432
# WORKER_PORT=3030
# WEB_PORT=3000 