# Romeo Docker Environment Configuration
# Copy this file to .env before running docker-compose

# OpenAI Configuration (Required for the worker service)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini

# Worker Service Configuration
LOG_LEVEL=info
CORS_ORIGIN=*

# Database Configuration (already set in docker-compose.yml)
# DATABASE_URL=postgresql://romeo:romeo_password@postgres:5432/romeo

# Optional: Port overrides (uncomment to change default ports)
# POSTGRES_PORT=5432
# WORKER_PORT=3030
# WEB_PORT=3000 