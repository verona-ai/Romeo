# Romeo AI Customer Service Agent - Environment Configuration

# ==============================================================================
# SERVER CONFIGURATION
# ==============================================================================

# Server port (default: 3000)
PORT=3000

# Node environment (development, production, test)
NODE_ENV=development

# Log level (error, warn, info, debug)
LOG_LEVEL=info

# CORS origin (use * for development, specific domains for production)
CORS_ORIGIN=*

# ==============================================================================
# DATABASE CONFIGURATION (PostgreSQL)
# ==============================================================================

# PostgreSQL connection string
# For development with Docker: postgresql://romeo:romeo_password@localhost:5432/romeo
# For production: postgresql://username:password@hostname:port/database
DATABASE_URL=postgresql://romeo:romeo_password@localhost:5432/romeo

# ==============================================================================
# OPENAI CONFIGURATION
# ==============================================================================

# OpenAI API key (required)
OPENAI_API_KEY=your-openai-api-key-here

# OpenAI model to use (default: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# Maximum tokens for responses (default: 1000)
MAX_TOKENS=1000

# Temperature for AI responses (0.0 to 1.0, default: 0.7)
TEMPERATURE=0.7

# ==============================================================================
# REDIS CONFIGURATION (Optional - for caching)
# ==============================================================================

# Redis connection URL (optional)
# For development with Docker: redis://localhost:6379
REDIS_URL=redis://localhost:6379

# ==============================================================================
# PRODUCTION DEPLOYMENT SETTINGS
# ==============================================================================

# For production deployments, consider these additional settings:

# Database connection pool settings
# DB_POOL_MIN=2
# DB_POOL_MAX=10

# Memory settings for large workloads
# MEMORY_LIMIT=512MB

# Security settings
# ENABLE_RATE_LIMITING=true
# MAX_REQUESTS_PER_MINUTE=100

# Monitoring and observability
# ENABLE_METRICS=true
# ENABLE_TRACING=true
